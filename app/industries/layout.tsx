import { metadata as industriesMetadata } from './metadata';

export const metadata = industriesMetadata;

export default function IndustriesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 